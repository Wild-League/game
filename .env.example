# Copy to .env for local development (cp .env.example .env)
# Or rely on defaults in src/api/base.lua when .env is absent.

ENV=dev
WORLD_API_URL=http://localhost:8000/v1/
WORLD_URL=http://localhost:3000/
NAKAMA_HOST=localhost
NAKAMA_PORT=7350
